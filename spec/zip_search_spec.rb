require 'spec_helper'

describe ZipSearch do
  it 'has a version number' do
    expect(ZipSearch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
